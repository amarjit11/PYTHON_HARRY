#all libraries
'''
Built in Libraries

math → mathematical functions (sqrt, factorial, etc.)
random → generate random numbers/choices
datetime → work with date and time
os → interact with operating system (files, folders)
sys → system-level operations (args, Python version)
time → time delays, timestamps
json → read/write JSON data
re → pattern matching (regular expressions)
collections → advanced data structures (Counter, deque)
itertools → efficient looping tools

'''

'''popular external library

NumPy → fast arrays & numerical operations
Pandas → data handling (tables like Excel)
Matplotlib → basic graphs & plots
Seaborn → advanced, prettier graphs
Requests → send HTTP requests (APIs)
Flask → lightweight web apps
Django → large web applications
Scikit-learn → ML models (regression, classification)
TensorFlow → deep learning & AI
PyTorch → deep learning (popular in research)

'''